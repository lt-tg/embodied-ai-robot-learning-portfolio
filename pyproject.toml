[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "embodied-ai-robot-learning-portfolio"
version = "0.1.0"
description = "Public embodied-AI robot learning portfolio with runnable demos and technical evidence."
requires-python = ">=3.8"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
